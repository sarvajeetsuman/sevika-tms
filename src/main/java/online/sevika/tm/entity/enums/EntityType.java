package online.sevika.tm.entity.enums;

/**
 * Enum representing different entity types in the system that can be audited
 */
public enum EntityType {
    USER,
    PROJECT,
    TASK,
    SUBSCRIPTION,
    SUBSCRIPTION_PLAN,
    PAYMENT
}
