package online.sevika.tm.entity.enums;

/**
 * Enum representing different audit actions that can be performed on entities
 */
public enum AuditAction {
    CREATED,
    UPDATED,
    DELETED,
    VIEWED,
    LOGIN,
    LOGOUT,
    STATUS_CHANGED,
    ASSIGNED,
    UNASSIGNED
}
